1) Tree with three nodes --> all one, order one, delete root  --> Fixed
2) Three nodes like one, delete one and add a few: Order 1 --> Fixed
3) Delete last two nodes --> Fixed
4) Order 2: delete node from leaf where all leafs and parent are at len(k)